# LP全ページ アンチAI改修計画

対象ルール: `docs/2026-05-29-anti-ai-lp-handoff.md`（17項目）+ `docs/2026-05-28-features-section-audit.md`
前提: TOP（`app/page.tsx`）は対応済み。残り18ページ（機能LP×10 + 業界LP×8）へ横展開。
将来: 最終的に静的HTML化してclassmethod.jpへ → コンポーネントはフラットなprops設計に留め、深いネスト/動的生成を避ける。

方針決定（ユーザー確認済み）:
- コンポーネント抽出範囲: Header/Footer + 信頼帯 + 最終CTA + 価格
- ベース背景: `--background` トークンを `#f5f5f5` にグローバル変更

---

## Phase 0 — 基盤トークン確定（globals.css）

- [ ] `--background` を `#ffffff` → `#f5f5f5` に変更（ガイド#7）
- [ ] 連動確認: 白背景前提の白カード/白テキストが破綻しないか（コントラスト）
- [ ] 既存トークンの確認のみ（変更不要）: `--link #2d8de0` / `--line-green-dark` / `univia-pro`
- [ ] TOPでインライン指定していた `#f5f5f5` をトークン参照に寄せる

## Phase 1 — 共通コンポーネント抽出（コンポーネント先行）

各コンポーネントを抽出する**その場でアンチAIルールを一度だけ適用** → 18ページへ自動伝播。

- [ ] `LpHeader`：18ページ重複の `<header sticky top-0 border-[#E5E7EB]>` を集約
  - ガイド#6: スマホ ハンバーガー化・縦並び
  - ファミリー差（サービス名/ナビリンク）は props で吸収
- [ ] `LpFooter`：18ページ重複の `<footer bg-[#0a0a0a]>` を集約
  - ガイド#7: ダーク→ライトトーン整理 / #6: スマホ縦並び
- [ ] `TrustBadgeBand`：信頼帯（全18ページに存在）
  - ガイド#9: ドット記号 → Awardアイコン+テキスト
- [ ] `FinalCtaSection`：最終CTA（全18ページに存在）
  - ガイド#17: 訴求コピー（「初回相談は無料です。」等）を太字強調 / #16: 強調の重複整理
- [ ] 価格: 機能LP×10は既存 `components/shared/feature-page/price-section.tsx` を踏襲。業界LPのインライン価格を同コンポーネントに寄せられるか検討（差が大きければ個別のまま）
- [ ] 各抽出後 `pnpm build` で型確認 + 1ページ差し替えて目視

## Phase 2 — ページ個別ルール適用（ファミリー単位バッチ）

コンポーネント化できない、各ページ固有のインライン部分。機能LP×10 → 業界LP×8 の順。同一ファミリー内は構造ほぼ同一なので同じdiffを横展開。

ページごとのチェック項目（ルール番号）:
- [ ] ヒーロー/暗色セクション `#0a0a0a`等 → `#f5f5f5`（#7）
- [ ] `text-line-green`/`#06C755` のコントラスト不足を修正（#3, audit P1）
- [ ] hex直書き `text-[#...]` → `muted-foreground`等トークン（#4）
- [ ] 画像 `<Image fill>` に `sizes` 指定（#5, audit P2）
- [ ] card grid の採否判断（#10。網羅目的なら残す）
- [ ] 不要なドロップシャドウ削除（#12）/ RECOMMENDED強調の重複整理（#16）
- [ ] 冗長なステップバッジ削除（#13）/ カテゴリラベルの位置・サイズ（#14）
- [ ] hover/focus-visible フィードバック（#15）
- [ ] ①②③箇条書き・重複説明・hero metric改行（#1, #11, audit）

## Phase 3 — 検証

- [ ] `pnpm build`（型チェック・全ページビルド通過）
- [ ] 各ページ目視 + コントラスト（AA 4.5:1）確認
- [ ] `main` との diff レビュー（バグ持ち込みなし）

---

## 進め方の提案

1. Phase 0 + Phase 1 を先に完了（基盤＋共通部品）。ここで18ページの重複修正が一括解決。
2. Phase 2 は機能LP 1ページを「お手本」として丁寧に仕上げ → 残り9ページへ横展開 → 業界LPも同様。
3. 横展開はページ単位で focused edit（haikuの複数Edit途中終了を避けるため、5件以上の繰り返しは1ページ=1まとまりで処理）。
